# 公共规范

公共规范适用于 HRM 的所有业务模块。模块 PRD 和用户故事默认引用这里的规则，不在模块内复制维护。

- [字段与表单校验规范](字段与表单校验规范.md)：必填、字段长度、格式、数值、附件、重复提交和错误处理。
- [权限与数据治理规范](权限与数据治理规范.md)：账号、角色、数据范围、权限、审计、软删除和生命周期。

新增公共规则时，需要确认它是否适用于所有模块；如果只适用于单个模块，应放在对应模块文档中并说明例外原因。
